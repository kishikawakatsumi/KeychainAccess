require 'spec_helper'

describe SwiftCoverage do
  it 'has a version number' do
    expect(SwiftCoverage::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
