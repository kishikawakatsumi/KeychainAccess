machine:
  xcode:
    version: "6.3.1"

dependencies:
  pre:
    - sudo easy_install cpp-coveralls
  override:
    - (cd Lib && bundle install --path=vendor/bundle --binstubs=vendor/bin)
  cache_directories:
    - 'Lib/vendor/bundle'

test:
  override:
    - (cd SwiftCov && script/cibuild)
